import { RialToTomanPipe } from './rial-to-toman.pipe';

describe('RialToToman.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new RialToTomanPipe();
    expect(pipe).toBeTruthy();
  });
});
